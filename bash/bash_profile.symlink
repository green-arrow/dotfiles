if [ -f ~/.bashrc ]; then
   source ~/.bashrc
fi

#OktaAWSCLI
if [ -f "~/.okta/bash_functions" ]; then
    . "~/.okta/bash_functions"
fi

export VOLTA_HOME="$HOME/.volta"
export PATH="$VOLTA_HOME/bin:$PATH"
. "$HOME/.cargo/env"
